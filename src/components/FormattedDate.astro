---
import { format } from "date-fns";

type Props = {
  date: Date;
};

const { date } = Astro.props;

const displayDate = format(date, "yyyy-MM-dd HH:mm");
---

<time datetime={date.toISOString()}>
  {displayDate}
</time>
