---
id: 2766
title: "CentOS下配置Node.JS"
pubDate: "2013-04-14 13:58:05"
category: "First Category"
---

首先安装[nvm](https://github.com/creationix/nvm)，这是个多版本管理器，也是目前装nodejs最优办法

```sh
yum install git
```

```sh
curl https://raw.github.com/creationix/nvm/master/install.sh | sh
```

```sh
echo ". ~/.nvm/nvm.sh" >> ~/.bashrc
```

关闭terminal，再打开

```sh
nvm install 0.10.4
```

再装[npm](https://npmjs.org/)，这是用来管理模块的

```sh
curl https://npmjs.org/install.sh | sh
```

安装express模块

```sh
npm install express
```

升级模块

```sh
npm update
```

资料看了一千条，环境搭了一百遍，一行代码没写，我又蛋痛了……
