---
import { getCollection } from "astro:content";
import Pagination from "../components/Pagination.astro";
import PostCard from "../components/PostCard.astro";
import { PAGE_PATH, POSTS_PER_PAGE } from "../consts.ts";
import Layout from "../layouts/Layout.astro";
import { calculateTotalPages } from "../utils/pagination";

const allPosts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime()
);
const totalPages = calculateTotalPages(allPosts.length);
const latestPosts = allPosts.slice(0, POSTS_PER_PAGE);
const page = 1;
---

<Layout>
  {
    latestPosts.map(async (post) => {
      return <PostCard post={post} />;
    })
  }
  <Pagination basePath={PAGE_PATH} page={page} totalPages={totalPages} />
</Layout>
